# StashTrack is its own git repository (first VSReacT consumer)
StashTrack/

# Local agent/tooling state — not part of the framework
.claude/
.agents/
docs/superpowers/
*.log
mitm_mcp_traffic.db

# Designer reference-art drops (zips + extractions); the plates each example
# actually uses are committed under its ui/src/assets
vsreact/examples/improvedUIs/

node_modules/
build*/
dist/
*.tsbuildinfo

# built package artifacts
vsreact/js/dist/
vsreact/js/*.tgz
CLAUDE.md

# generated per-example: base64-inlined assets + screenshot-verify captures
vsreact/examples/*/ui/src/_assets.ts
vsreact/examples/*/ui/shot*.png

# site screenshots from tools/capture-site.mjs
.shots/
dist-plugins/
